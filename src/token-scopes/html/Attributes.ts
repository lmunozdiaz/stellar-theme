import { syntax } from "../../colors";

export const attributes = [
  {
    name: "HTML Attribute",
    scope: ["entity.other.attribute-name"],
    settings: {
      foreground: syntax.attributes,
    },
  },
];
